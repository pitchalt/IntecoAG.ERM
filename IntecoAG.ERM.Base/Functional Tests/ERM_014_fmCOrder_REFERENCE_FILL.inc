*Action Navigation(Finance.fm COrder)

; Не работает, т.к. есть поле, обязательно для ввода, но отсутствующее в интерфейсе и его пожетому нельзя ввести

;*Action New
;*FillForm
; Description = Order 1
