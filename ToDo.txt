Список задач по системе IntecoAG.ERM


+1. Составить список контроллеров, какой из них за что отвечает.

+2. Слить контроллеры для утверждения версии и создания новой версии

+3. Слить контроллеры, сущность которых сводится к собственной навигации по системе.

4. Как-то сделать, чтобы отслеживалась история переходов по mini-навигации с целью возврата по ней.

+5. Очистка каталога проекта Module от лишних файлов

+6. Appearance в форме регистрации для disable поля со списком.

7. Найти способ моментального получения значения из базы и немедленного сохранения его нового значения. 
Подходящий атрибут найти пока не удалось (кроме Key, который не всегда подходит), другой способ - применять сразу
ExecuteScalar и т.п., третий - вместо счётчиков использовать генератор, чнтвёртый - перехват события сессии на изменение объекта (а дальше непонятно)Ю
пятый - получать номер в контроллере (непонятна пока реализация). Поиск продолжается...

+8. Оптимизировать функциональность контроллеров и их состав (в принципе - постоянная доработка).


9. Создать контроллер, отвечающий за создание нового члена в коллекции, являющейся свойством объекта с таким расчётом, чтобы учитывался контекст
события создания.

10. Не совсем корректная работа собственного фильтра (кнопка фильтра не приводит фильтр в начальное состояние по отношению
показываемых видов фильтров).

11. Приведение кодов в приличное состояние.

+12*. Записать сценарий полной работы с простым договором начиная с пустой базы.
	- +Вызов скриптов друг из друга;
	- +Один стартовый скрипт, вызывающий в нужном порядке остальные;
	- +Локализация: её влияние на построение скриптов (название акций); http://www.devexpress.com/Support/Center/p/Q321758.aspx
	- +Управление путями для тестовой базы (не удаётся сделать относительный путь);
	- +Перемещение тестов в другой каталог (переместил Павел).

+13. Исследование механизма извлечения данных из БД (XPO и XAF).

+14. Как поддерживать свойство, значение которого является итогом вычисления во вложенной коллекции. Найдена и передана ссылка из формума.

+15. Исправление работы контроллеров VersionServiceController и ListViewFilterPanelController (работал на неверсионных объектах) (не формровалась панель фильтра).

+16. Пока есть вопрос, как вызвать конструктор с параметрами, если известен тип в виде экземпляра Type?
[0:08:11] Влад: Не очень врубился в вопрос. Насколько я понял, речь идёт о создании нового объекта и надо его создать 
именно с заданным конструктором, при этом имеется экземпляр типа объекта, т.е. сам объект имеет тип T и есть объект Obj, 
определённый так:  Type Obj = T. Верно ли я понял?
[0:10:37] Павел Узорин: Нет, есть экземляр Type, описывающий тип, а сам тип имеет конструктор с параметром, вопрос: 
как вызват ьконструктор, если известен только экземпляр Type?

+17. Скрипты EasyTest на нелокализованной системе.

+18. Кнопка New должна при нажатии показывать выпадающий список объектов, если их более одного, иначе сразу срабатывать.

+19. Создать NUnit-тесты для операции создания простого договора без этапов.

+20. Дополнение 11: проверить коды на предмет корректной работы сессий и uow для получения номеров счётчиков.

+21. Не работает в выпадающих списках контроллер по управлению кнопкой New с выпадающим списком типов создаваемых объектов: 
выполнять сразу, если выпадающий список содержит один элемент или показывать сам выпадающий список - при нажатии на основную 
часть кнопки (не стрелку). Результат: реализация маловероятна: см. http://www.devexpress.com/Support/Center/p/Q329102.aspx - 
свежак от июня 2011 года в точности по этому вопросу.

22. 
